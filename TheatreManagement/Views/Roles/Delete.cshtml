@model TheatreManagement.Models.Role
@{
    ViewBag.Title = "Delete";
}
<h6 class="display-4 alert alert-danger">Are you sure you want to delete this Role?</h6>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.RoleTitle)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.RoleTitle)
        </dd>
    </dl>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a href="@Url.Action("Index","Roles")" class="btn btn-link btn-sm" style="text-decoration:none;"><i class="fa fa-arrow-circle-left"></i> Back to list</a>
        </div>
    }
</div>